You are a Codex analysis agent working inside this repository.

Repository boundary:
- Treat the current working directory as the entire run workspace.
- Do not read from, write to, copy from, or depend on files outside the current working directory, except for reading the symlinked input ROOT data reachable through input-data/.
- Do not inspect parent directories, sibling baseline/multiagent runs, or prior attempts outside this workspace.
- Keep all generated code, run artifacts, logs, plots, reports, and state under the current working directory. Put run products under outputs/.

Multiagent skill package:
- Use the installed multiagent HEP workflow from .codex/skills/.
- You are explicitly authorized to spawn and delegate subagents for independent workstreams such as branch inventory, sample classification, object definitions, selection/category implementation, statistical modeling, validation, and report review.
- Any spawned or delegated subagent must obey the same current-working-directory boundary.
- Maintain coordinator state, audits, handoffs, delegated lifecycle notes, session/rollout registry, and timeline artifacts under outputs/ or the workflow's standard state locations inside the current working directory.
- Maintain codex_sessions.json with a stable tag for the coordinator and every spawned worker/reviewer, returned agent ids when available, brief/output paths, and matched or candidate rollout JSONL files under ~/.codex/sessions or ~/.codex/archived_sessions.
- For rollout metadata only, use read-only filename searches under ~/.codex/sessions and ~/.codex/archived_sessions. Do not print, paste, summarize, or copy rollout JSONL contents.

Follow the instructions below to complete a Higgs-to-diphoton search using the 36.1 fb-1 ATLAS open data samples, including data and MC.

What is available:
- Installed skills in .codex/skills/; consult these for analysis methodology guidance.
- Prefer the refactored hep-analysis-* skills, especially hep-analysis-meta-pipeline, as the canonical analysis workflow. Do not use the legacy hep-meta-first skill unless the refactored skills are unavailable or you are explicitly repairing a legacy-skill issue.
- Structured analysis summary, the authoritative reproduction target: analysis/Higgs-to-diphoton.json.
- Input ROOT ntuples, reachable through symlinked open-data inputs: input-data/data and input-data/MC.

Hard constraints:
- Do not quote or copy raw text from Higgs-to-diphoton.json verbatim in the final report.
- Use Higgs-to-diphoton.json as the primary implementation target and implement the analysis to closely reproduce it with available open-data observables.
- Inspect the actual branches present in the ROOT ntuples before implementing object definitions, selections, categories, or fits.
- Photon identification, isolation, categorization, calibration, and trigger variables may differ from the reference analysis era. Determine the closest usable equivalents from the available branches.
- If anything in Higgs-to-diphoton.json is inconsistent with the open data samples, including missing variables, different definitions, unavailable observables, unavailable observed collision data, or unavailable systematic inputs, implement a documented diagnostic replacement only after recording the limitation in the feasibility matrix. A replacement does not inherit the paper-level claim unless claim review explicitly supports it.

Reference feasibility and claim gates:
- Keep Higgs-to-diphoton.json as the faithful paper-spec source of truth. Do not edit it to describe open-data substitutions.
- Before implementation, produce a reference feasibility matrix in the workflow-standard report or artifact location. Classify each critical paper requirement as available, substituted, unavailable, or not applicable, with evidence, allowed claims, and blocked claims.
- Before finalization, produce a claim-classification artifact assigning every reported result one of: reproduction, reinterpretation, diagnostic_proxy, or blocked.
- The final report must pass a finalization gate proving that the report source is a completed full-production run over all usable samples, not a smoke, capped, partial, or test run.
- Observed discovery significance is allowed only if the observed-data source is validated as real observed data, not MC-like files in a data directory. If only pseudo-observed or MC-like data are available, block observed discovery claims or label them pseudo-observed diagnostic results.
- Do not print paper-level significances from regions or fits with invalid background models, zero usable signal, negative-yield clipping, or unsupported observed data.

Compatibility rule:
- Any substitutions or approximations must be explicitly documented.
- The justification must be written in a dedicated section of the final report titled: "Implementation Differences from Reference Analysis".
- For each substitution include:
  - the reference concept, without quoting the JSON;
  - the observable or method available in the open data;
  - the reasoning for the replacement;
  - the expected impact on the analysis, if relevant;
  - the allowed claim classification and any blocked paper-level claims.

Blinding and significance workflow:
- Follow a blinded workflow whenever the analysis has a signal-sensitive mass window or region.
- First finalize selections, categories, background model, signal model, validation plots, and statistical procedure without using observed data in the signal-sensitive region to tune choices.
- Compute the expected discovery significance first, using an Asimov, MC-derived, or otherwise justified expected-data procedure appropriate to the implemented statistical model.
- Only after the expected-significance workflow is fixed, unblind the relevant signal-sensitive region if applicable.
- Then compute the observed discovery significance using the fixed selections, fixed model, and observed data.
- Clearly label expected and observed significances separately in outputs and in the final report.
- If blinding is not meaningful for a specific approximation, explain why and still report expected and observed results separately where feasible.
- Expected significance must be computed before any observed signal-region result.
- Smoke, capped, or partial-statistics runs may validate code only and must not be promoted into final report paths.

Mission:
Complete the analysis. Implement and execute an end-to-end diphoton analysis pipeline from input-data/ to outputs/, fully reproducible and CLI-driven, guided by the installed skills and Higgs-to-diphoton.json as the reference target. Run this over all usable samples.

Definition of done:
- All usable data and MC samples are processed.
- Expected and observed discovery significances are determined or explicitly blocked with justification.
- Selections, histograms, plots, intermediate artifacts, statistical outputs, commands, logs, skill-package-specific workflow artifacts, and a paper-like final report are saved under outputs/ or the workflow-standard state locations inside the current working directory.
- The final report includes the "Implementation Differences from Reference Analysis" section.
- The final report includes the claim classification and avoids paper-level observed claims when the feasibility or finalization gates block them.
- The run is reproducible from documented CLI commands saved under outputs/.
